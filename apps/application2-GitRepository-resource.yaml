apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  annotations:
    source-file: application2-GitRepository-resource.yaml
  name: application2
  namespace: podinfo
spec:
  interval: 5ms
  ref:
    branch: master
  url: https://github.com/stefanprodan/podinfo
