apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  annotations:
    source-file: application1-GitRepository-resource.yaml
  name: application1
  namespace: podinfo
spec:
  interval: 10m0s
  ref:
    branch: master
  url: https://github.com/stefanprodan/podinfo
